# Delay before the popup retakes focus after a platform insertion that moved it.
INSERTION_POPUP_REFOCUS_DELAY_MS = 80

# Wait for the physical hotkey keys to be released before native insertion.
INSERTION_KEY_RELEASE_POLL_INTERVAL_MS = 10
INSERTION_KEY_RELEASE_MAX_ATTEMPTS = 50
